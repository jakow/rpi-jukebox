PlayerService
  play
  pause
  seek
  backward/forward
  auto-increment seekbar
  getqueue
  server events/interval polling?

SearchService
  YT search
  local search w/ AJAX prefetch

PlaylistService
  compose playlists to be saved for later. Backend first



CSS:
  make sure that button presses reflect the fact things are happening

Song data to be sent over to server:
  YT - by ID
  local - assign IDs?


Error informations for user
